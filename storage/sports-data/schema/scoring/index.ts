/**
 * Scoring System
 * 
 * Provides generic types and interfaces for scoring methods.
 * 
 * Scoring method implementations are provided by sport-specific modules
 * (e.g., golf, tennis) and registered via the registry.
 */

export * from './base';
export * from './registry';

